name: luals-checker

on:
  push:
    branches:
      - main
    tags:
      - "*"
  pull_request:
    branches:
      - main
  workflow_dispatch:


run-name: Test LuaLS
jobs:
  LuaLS-checker:
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello, world why is this not running?"
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          path: ./workspace
      - name: LuaLS check
        uses: DeadlyBossMods/LuaLS-config@main
        with:
          luals-repo: emmericp/lua-language-server
          luals-ref: 606e9cd238dbc924929d7047497de30ff44ee50c
          luals-check-dir: ${{ github.workspace }}/workspace
